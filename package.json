{
  "name": "talksyraproject",
  "version": "1.0.0",
  "private": true,
  "description": "TalkSyra Cloudflare Worker project (minimal package.json to satisfy CI)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/worker.js",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {}
}
